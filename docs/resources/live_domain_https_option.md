---
page_title: "Resource alicloudextend_live_domain_https_option - alicloudextend"
subcategory: "ApsaraVideo Live"
description: |-
  Manages the HTTPS options (HTTP/2) for an ApsaraVideo Live domain. Requires HTTPS to be enabled via alicloudextend_live_domain_certificate first.
---

# alicloudextend_live_domain_https_option

Manages the HTTPS options (HTTP/2) for an ApsaraVideo Live domain. Requires HTTPS to be enabled via alicloudextend_live_domain_certificate first.

## Example Usage

```terraform
resource "alicloudextend_live_domain_certificate" "example" {
  domain_name  = "play.example.com"
  ssl_protocol = "on"
  cert_type    = "upload"
  ssl_pub      = file("path/to/cert.pem")
  ssl_pri      = file("path/to/key.pem")
}

resource "alicloudextend_live_domain_https_option" "example" {
  domain_name = "play.example.com"
  http2       = "on"

  depends_on = [alicloudextend_live_domain_certificate.example]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `domain_name` (String) The live domain name.
- `http2` (String) Whether to enable HTTP/2. Valid values: on, off.



## Import

Import is supported using the domain name:

```shell
terraform import alicloudextend_live_domain_https_option.example play.example.com
```
